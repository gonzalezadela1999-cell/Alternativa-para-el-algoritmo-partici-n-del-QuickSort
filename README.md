Implementar el algoritmo QuickSort en Java utilizando el último elemento como pivote. El programa debe incluir una clase principal con método main, un método quickSort, un método partition y un método printArray para mostrar el arreglo antes y después de ordenarlo.

Luego implementar una variante de QuickSort donde el pivote se seleccione en la posición media del arreglo o subarreglo.
